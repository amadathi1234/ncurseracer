# ncurseracer: a typing game implemented using the ncurses library
Use the makefile or compile with g++ (use compiler flag -lncurses)

Created by Anand Madathil and Siddarth Pillai as a project for CS368
